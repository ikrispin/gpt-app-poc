# gpt-app-poc Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-02-02

## Active Technologies
- TypeScript (Node.js 18+), ES modules + `@modelcontextprotocol/sdk`, `@modelcontextprotocol/ext-apps`, `express`, `zod`, `cors`, `vite`, `tsx` (003-chatgpt-app-technical-readiness)
- N/A (no persistence) (003-chatgpt-app-technical-readiness)
- TypeScript 5.x on Node.js 18+ (ES modules) + `express`, `@modelcontextprotocol/sdk`, `@modelcontextprotocol/ext-apps`, `zod`, `cors` (004-jira-attachment-pat)
- Backend token vault persistence with encrypted-at-rest credential records (user-scoped), plus non-secret audit/event records (004-jira-attachment-pat)
- TypeScript (Node.js 18+) + @modelcontextprotocol/sdk, @modelcontextprotocol/ext-apps, Express, existing server stack (unchanged for Jira) (005-mcp-skill-discovery)
- Repo-local file only — `SKILL.md` on disk (path TBD under repo root or `specs/`); no new persistence (005-mcp-skill-discovery)
- TypeScript (Node.js 18+) + `@modelcontextprotocol/sdk`, `@modelcontextprotocol/ext-apps`, `express`, `zod`, Node built-ins (`fs/promises`, `crypto`, `path`, `child_process`) (006-sosreport-local-flow)
- Local filesystem only (existing archive path + `/tmp` copied archive); no new database/persistent secrets (006-sosreport-local-flow)
- TypeScript (Node.js 18+) + `@modelcontextprotocol/sdk`, `@modelcontextprotocol/ext-apps`, `express`, `zod`, `vite`, Node built-ins (007-engage-red-hat-support)
- Existing local filesystem artifact flow (`/tmp`) and existing encrypted token vault/lifecycle store; no new persistence layer (007-engage-red-hat-support)
- TypeScript on Node.js (current repo baseline) + `@modelcontextprotocol/sdk`, `@modelcontextprotocol/ext-apps`, `express`, `tsx`, `vite` (008-switch-app-endpoint)
- N/A for this change (no data model persistence changes) (008-switch-app-endpoint)
- Existing filesystem-based skill markdown loading; no new persistence (009-get-skill-tool)
- Existing token vault and lifecycle store for opaque `connection_id`; filesystem-based sos artifacts (010-engage-support-workflow)
- TypeScript (Node.js 18+) + `@modelcontextprotocol/sdk`, `@modelcontextprotocol/ext-apps`, `express`, `zod`, `node:crypto`, `tsx` (011-consent-gate-sosreport)
- Existing in-memory/runtime stores plus new consent nonce consumption store (single-use state keyed by token `jti`) (011-consent-gate-sosreport)
- TypeScript 5.9 (Node.js runtime for server, browser bundle for widget UI) + Existing MCP/Express stack plus `react`, `react-dom`, `@patternfly/react-core`, `@patternfly/react-icons` (012-patternfly-ui-swap)
- N/A for this increment (no new persistence) (012-patternfly-ui-swap)
- TypeScript (Node.js, ESM) + `@modelcontextprotocol/sdk`, `@modelcontextprotocol/ext-apps`, `express`, `zod`, `node:crypto`, `tsx` (013-mcp-consent-mint-path)
- Existing in-memory workflow/session and consent single-use state (013-mcp-consent-mint-path)
- TypeScript (Node.js, ESM) + `@modelcontextprotocol/sdk`, `@modelcontextprotocol/ext-apps`, `express`, `zod`, `tsx` (014-headless-consent-compat)
- Existing in-memory workflow/session and consent state; no new persistence required (014-headless-consent-compat)
- TypeScript (Node.js ESM) + Markdown/JSON contract artifacts + `@modelcontextprotocol/sdk`, `@modelcontextprotocol/ext-apps`, `express`, `zod`, `tsx --test` (015-engage-support-split)
- Existing in-memory workflow state + existing backend secure token vault; no new datastore (015-engage-support-split)
- TypeScript (Node.js ESM) + Markdown/YAML/JSON planning artifacts + `express`, `zod`, `@modelcontextprotocol/sdk`, `tsx --test` (016-jira-cloud-auth)
- Existing file-backed connection lifecycle store and encrypted token vault under `.data/` (016-jira-cloud-auth)
- TypeScript 5.9 + React 19 + `@patternfly/react-core`, `@patternfly/react-icons`, `@modelcontextprotocol/ext-apps`, `vite` (017-rhds-visual-alignment)
- N/A for Step 0 (no new persistence) (017-rhds-visual-alignment)
- TypeScript + React (existing project baseline) + `@patternfly/react-core`, RHDS component library (incremental hybrid usage), existing app state/types in `src/mcp-app` (018-rhds-hybrid-migration)
- N/A (UI migration only; no data model persistence change) (018-rhds-hybrid-migration)
- TypeScript (ESM) + React 19 in existing Vite build pipeline + `react`, `react-dom`, `@modelcontextprotocol/ext-apps`, `@modelcontextprotocol/sdk`, current `@patternfly/react-core` and `@patternfly/react-icons` targeted for retirement (019-rhds-final-migration)
- N/A (UI migration only) (019-rhds-final-migration)
- TypeScript 5.x (ESM) on Node runtime + `@modelcontextprotocol/sdk`, `@modelcontextprotocol/ext-apps`, `zod`, `express` (020-cpu-information-mcp)
- N/A (ephemeral local diagnostics read at call time) (020-cpu-information-mcp)
- TypeScript 5.x (ESM), React 19.x, Node.js runtime + `@modelcontextprotocol/sdk`, `@modelcontextprotocol/ext-apps`, `react`, `react-dom`, `zod`, `express` (021-cpu-info-ui-step)
- N/A (workflow state is in-memory/session-scoped) (021-cpu-info-ui-step)
- TypeScript 5.x (ESM), React 19.x, Node.js runtime + `@modelcontextprotocol/sdk`, `@modelcontextprotocol/ext-apps`, `react`, `react-dom`, `express`, `zod` (022-cpu-resource-ui)
- In-memory session-scoped buffers and interval jobs (no persistent storage) (022-cpu-resource-ui)
- TypeScript 5.x (ESM), Node.js runtime + `@modelcontextprotocol/sdk`, `express`, `zod` (023-ocp-admin-persona)
- N/A — no persistent or in-memory state for this feature (023-ocp-admin-persona)
- N/A — mock data embedded in source; no persistent state (024-ocp-admin-ui)
- N/A — mock data served from server-side constants (025-ocp-admin-interactive-ui)
- TypeScript 5.x (ESM), Node.js runtime + `@modelcontextprotocol/sdk` (Client, StdioClientTransport), `express`, `zod` (026-ocp-mcp-integration)
- In-memory connection cache (no persistence) (026-ocp-mcp-integration)

- TypeScript (Node.js 18+) + @modelcontextprotocol/sdk, @modelcontextprotocol/ext-apps, Express, cors, Vite, vite-plugin-singlefile, tsx (001-mcp-apps-hello-world)

## Project Structure

```text
src/
tests/
```

## Commands

npm test && npm run lint

## Code Style

TypeScript (Node.js 18+): Follow standard conventions

## Recent Changes
- 026-ocp-mcp-integration: Added TypeScript 5.x (ESM), Node.js runtime + `@modelcontextprotocol/sdk` (Client, StdioClientTransport), `express`, `zod`
- 025-ocp-admin-interactive-ui: Added TypeScript 5.x (ESM), React 19.x, Node.js runtime + `@modelcontextprotocol/sdk`, `@modelcontextprotocol/ext-apps`, `react`, `react-dom`, `express`, `zod`
- 024-ocp-admin-ui: Added TypeScript 5.x (ESM), React 19.x, Node.js runtime + `@modelcontextprotocol/sdk`, `@modelcontextprotocol/ext-apps`, `react`, `react-dom`, `express`, `zod`


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
